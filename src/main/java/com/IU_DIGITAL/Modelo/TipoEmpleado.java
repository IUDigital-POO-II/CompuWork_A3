package com.IU_DIGITAL.Modelo;


// Enumeracion que define los tipos de empleados
public enum TipoEmpleado {
    PERMANENTE, // Empleado con contrato indefinido
    TEMPORAL   // Empleado con contrato por tiempo determinado
}
